# Worker-only profile - disables web server
# Use with: -Dspring-boot.run.profiles=e2e,worker

spring:
  main:
    web-application-type: none
  flyway:
    enabled: false  # UI handles migrations

commandbus:
  worker:
    auto-start: true
